diff --git a/kernel/runtime/ksud.c b/kernel/runtime/ksud.c
index a18707b..00ad390 100644
--- a/kernel/runtime/ksud.c
+++ b/kernel/runtime/ksud.c
@@ -53,8 +53,10 @@ void on_post_fs_data(void)
     ksu_load_allow_list();
     ksu_observer_init();
     // sanity check, this may influence the performance
     stop_input_hook();
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 10, 0)
     ksu_selinux_hide_handle_post_fs_data();
+#endif
 }
 
 extern void ext4_unregister_sysfs(struct super_block *sb);
@@ -216,7 +218,9 @@ int ksu_handle_execveat_ksud(int *fd, struct filename **filename_ptr,
             char buf[16];
             if (check_argv(*argv, 1, "second_stage", buf, sizeof(buf))) {
                 pr_info("/system/bin/init second_stage executed\n");
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 10, 0)
                 ksu_selinux_hide_handle_second_stage();
+#endif
                 apply_kernelsu_rules();
                 cache_sid();
                 setup_ksu_cred();
@@ -288,7 +292,9 @@ int ksu_handle_execveat_ksud(int *fd, struct filename **filename_ptr,
         char buf[16];
         if (!init_second_stage_executed && check_argv(*argv, 1, "second_stage", buf, sizeof(buf))) {
             pr_info("/system/bin/init second_stage executed\n");
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 10, 0)
             ksu_selinux_hide_handle_second_stage();
+#endif
             apply_kernelsu_rules();
             cache_sid();
             setup_ksu_cred();
diff --git a/kernel/selinux/rules.c b/kernel/selinux/rules.c
index f8a33fa..778cd58 100644
--- a/kernel/selinux/rules.c
+++ b/kernel/selinux/rules.c
@@ -254,6 +254,9 @@ do_stop_machine:
 out_flush:
     smp_mb();
     reset_avc_cache();
+#ifdef CONFIG_KSU_SUSFS
+    susfs_set_batch_sid();
+#endif
 #endif
 }
 
diff --git a/kernel/sulog/event.c b/kernel/sulog/event.c
index 144b375..15c4b9a 100644
--- a/kernel/sulog/event.c
+++ b/kernel/sulog/event.c
@@ -373,9 +373,14 @@ static struct ksu_sulog_pending_event *ksu_sulog_capture_grant_root(const struct
     struct ksu_sulog_event *event;
 
     // This is actually stupid fix
+#ifdef CONFIG_KSU_SUSFS
     #define USER_ARG_NULL user_arg_null_ptr()
+#else
+    #define USER_ARG_NULL (*user_arg_null_ptr())
+#endif
 
     pending = ksu_sulog_capture(KSU_SULOG_EVENT_IOCTL_GRANT_ROOT, NULL, USER_ARG_NULL, gfp);
+    #undef USER_ARG_NULL
     if (!pending)
         return NULL;
 
